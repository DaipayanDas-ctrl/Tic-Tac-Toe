# Public URL of the Socket.IO backend.
# Leave unset to default to http://localhost:3001 (local development).
# When the frontend is deployed on Vercel, set this to the deployed backend
# URL, e.g. https://tic-tac-toe-server.onrender.com
VITE_SERVER_URL=
